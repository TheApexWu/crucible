# UI Test Variation Plans (EnneaFlow)

## Variation 1: Replay-First (Warm Asymmetrical Discovery)
Plan:
- Keep CRUCIBLE round replay intent from the main repo: sequence through split/steal outcomes and deception index shifts.
- Apply EnneaFlow constraints: cream base, sharp edges, DM Sans-heavy headline, rotated hero geometry, hard drop shadows.
- Verify both visual system and behavior: style token checks plus round-state transition checks.

Carried out:
- Built `fixtures/variation-01-replay.html` + shared `fixtures/enneaflow.css`.
- Implemented test in `tests/variation-01-replay-navigation.spec.ts`.

## Variation 2: Analysis-First (Event Spine)
Plan:
- Adapt CRUCIBLE strategy analysis into an Ennea-Line vertical event stack.
- Ensure the 2px terracotta connecting line and square icon nodes drive interaction.
- Validate that clicking event nodes updates detail context.

Carried out:
- Built `fixtures/variation-02-analysis.html` with clickable strategy events.
- Implemented test in `tests/variation-02-metrics-integrity.spec.ts`.

## Variation 3: Ops/Observability (Datadog-facing)
Plan:
- Align with README observability scope (Datadog + deception metrics) as a monitoring-focused surface.
- Keep EnneaFlow system consistency: sticky cream header, sharp CTA, warm dark footer, hover motion on CTA arrow.
- Validate alert-state behavior and key style constraints.

Carried out:
- Built `fixtures/variation-03-ops.html`.
- Implemented test in `tests/variation-03-analysis-linkage.spec.ts`.
