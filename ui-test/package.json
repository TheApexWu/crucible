{
  "name": "ui-test",
  "version": "0.1.0",
  "private": true,
  "description": "Frontend UI test variations for the CRUCIBLE demo",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "typescript": "^5.9.2"
  }
}
